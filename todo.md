# TODO

- [ ] Update all app images/assets to use YDEX branding (replace remaining Deriverse logos in `/assets`, update `MobileRestrictedView` hero image, verify favicon/social previews).
- [ ] Create Supabase database for the app (provision project, set env vars, apply schema from `Documents/database.md`, verify tables for trades, annotations, wallets).
