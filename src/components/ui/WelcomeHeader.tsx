'use client';

/**
 * WelcomeHeader component for onboarding surfaces (Welcome + Wallet Ask).
 * 
 * Empty header to remove the redundant logo from the top-left.
 */
export default function WelcomeHeader() {
    return null;
}
